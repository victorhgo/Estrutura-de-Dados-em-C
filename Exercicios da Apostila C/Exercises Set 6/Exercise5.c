/* 6.1.5 Preencha uma matriz quadrada de ordem 4, da seguinte
forma:
        se i <> j, m[i, j] = 0 e
        se i = j, m[i, j] = 1                              */

#include <stdio.h>

int main(){
// In order to perform this operation, we need a for loop with
// an if/else condition to fill the matrix:

}